# Sesión de trabajo — 20 de junio de 2026

## Objetivo del día

Ajustar la tipografía de los **precios** en la sección de membresías: sustituir Bebas Neue por **Cabinet Grotesk** en `.pricing-card__amount`, alineado con el resto de títulos y display de `landing-1`.

**Alcance:** `landing-1.css`, `landing-3.css`.

**Producción:** `https://walkdogentrena.com/landing-1.html`

---

## Archivos modificados

| Archivo | Acción |
|---------|--------|
| `landing-1.css` | Modificado — `font-family` de `.pricing-card__amount` |
| `landing-3.css` | Modificado — mismo cambio en `.pricing-card__amount` |

---

## 1. Precios de membresías — Cabinet Grotesk

### Cambio aplicado

```css
.pricing-card__amount {
  font-family: "Cabinet Grotesk";
}
```

**Antes:** `font-family: "Bebas Neue", sans-serif;`

### Contexto

- En `landing-1.css` ya existía `--font-display: "Cabinet Grotesk", Inter, sans-serif` y el plan intermedio (`.pricing-card--plan-2 .pricing-card__amount`) usaba `var(--font-display)`.
- La regla base de `.pricing-card__amount` seguía en Bebas Neue; ahora los tres planes ($50, $100, $1000) comparten Cabinet Grotesk.

### Fuentes cargadas

| Landing | Fuente display | CDN |
|---------|----------------|-----|
| `landing-1.html` | Cabinet Grotesk + Inter | Fontshare + Google Fonts |
| `landing-3.html` | Inter + Bebas Neue | Google Fonts |

**Pendiente en `landing-3`:** si se usa esa landing en producción, añadir Cabinet Grotesk en el `<head>` (Fontshare, igual que `landing-1.html`) para que el cambio CSS se renderice correctamente.

---

## 2. Despliegue

### Archivos mínimos

```
landing-1.css
landing-3.css   (solo si landing-3 está en producción)
```

### Verificación tras subir

1. **Ctrl+F5** en la sección `#membresias`.
2. Precios $50 / $100 / $1000 en **Cabinet Grotesk** (no Bebas Neue).
3. Consistencia visual con h1, h2 y demás elementos que usan `--font-display`.

---

## Checklist rápido post-despliegue

- [ ] Subir `landing-1.css` (y `landing-3.css` si aplica)
- [ ] Ctrl+F5 — revisar precios en cards de planes
- [ ] Confirmar que Cabinet Grotesk carga (pestaña Red → fontshare o Network)

---

## Resumen en una línea

Precios de membresías unificados en Cabinet Grotesk (`.pricing-card__amount`) en `landing-1.css` y `landing-3.css`.

---

*Sesión: 20 de junio de 2026 | walkdogentrena.com*
